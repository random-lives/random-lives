{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "jekyll",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "cd /Users/damonbinder/Documents/RandomLivesWebsite && export PATH=/opt/homebrew/opt/ruby/bin:$PATH && bundle exec jekyll serve --unpublished --port 4000"],
      "port": 4000
    }
  ]
}
